version: '3.8'

networks:
  area_network:
    driver: bridge

volumes: {}
  # Add volumes for your microservices here
  # Example: servicename_db_data:

services:
  # Microservices will be added here as they are created
  # Use: make new-service NAME=servicename PORT=808X
  # Template for adding a new service:
  #
  # servicename-service:
  #   build:
  #     context: ./Services/servicename
  #     dockerfile: Dockerfile
  #   container_name: area_servicename_service
  #   ports:
  #     - "${SERVICENAME_SERVICE_PORT:-8081}:${SERVICENAME_SERVICE_PORT:-8081}"
  #   environment:
  #     DB_HOST: servicename-db
  #     DB_PORT: 5432
  #     DB_USER: ${DB_USER:-postgres}
  #     DB_PASSWORD: ${DB_PASSWORD:-postgres}
  #     DB_NAME: ${SERVICENAME_DB_NAME:-servicename_db}
  #     SERVER_PORT: ${SERVICENAME_SERVICE_PORT:-8081}
  #   depends_on:
  #     servicename-db:
  #       condition: service_healthy
  #   networks:
  #     - area_network
  #   restart: unless-stopped
  #
  # servicename-db:
  #   image: postgres:16-alpine
  #   container_name: area_servicename_db
  #   environment:
  #     POSTGRES_USER: ${DB_USER:-postgres}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
  #     POSTGRES_DB: ${SERVICENAME_DB_NAME:-servicename_db}
  #   ports:
  #     - "${SERVICENAME_DB_PORT:-5432}:5432"
  #   volumes:
  #     - servicename_db_data:/var/lib/postgresql/data
  #     - ./Services/servicename/db/init:/docker-entrypoint-initdb.d
  #   networks:
  #     - area_network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped
  #
  # Note: Don't forget to add the volume at the top of the file:
  # volumes:
  #   servicename_db_data:
