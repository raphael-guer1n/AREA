version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: poc-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: poc_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
